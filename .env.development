# AI Top Trumps Card Generator - Development Environment Configuration
# This file contains development-specific settings

# Gemini API Configuration
# Get your API key from: https://aistudio.google.com/app/apikey  
GEMINI_API_KEY=your_gemini_api_key_here

# Application Environment
NODE_ENV=development

# Development Server Configuration
PORT=3001

# Frontend API Configuration (points to local development server)
VITE_GEMINI_API_URL=http://localhost:3001/api/generate

# Google Cloud Storage Configuration (for local development)
# Note: In development, local file storage is used instead of Google Cloud Storage
# These settings are kept for compatibility but are not actively used
STORAGE_BUCKET=dev-local-storage
GOOGLE_APPLICATION_CREDENTIALS=server/google-cloud-key.json

# Development-specific settings
# Rate limiting is disabled in development
# Logging uses local file system instead of cloud storage
# Authentication is more permissive in development

# Development Notes:
# - Rate limiting is bypassed for easier testing
# - Cloud storage is replaced with local file storage in dev-storage/
# - Logging is simplified to prevent cloud storage errors
# - All rate limit headers are still generated for testing